# Optional: only needed for production (e.g. Vercel).
# In development, the app uses the Vite proxy to localhost:5000.
#
# Set this to your backend URL when deploying the frontend (e.g. on Vercel).
# Example: https://your-backend.up.railway.app
# VITE_API_URL=

# Razorpay (public key for checkout)
# VITE_RAZORPAY_KEY_ID=
